{{main_products}}

Технічне завдання 
на {{pages_count}} сторінках


1. Виконавець зобов'язується реалізувати наступне технічне завдання:

1.1. Створити Інтернет-сайт на {{cms}} в домені {{domain}}. Домен надається {{customer_name}}. Виконавець повинен розробити і надати замовнику макет дизайну Інтернет-магазину, {{backend_language}}-програму, яка реалізовує вимоги Замовника, та {{database_type}}-базу даних, де повинна зберігатися і оновлюватися вся необхідна для роботи {{backend_language}}-програми інформація.

1.2. Сайт відображається на наступних мовах: {{ languages|join(', ') }}.

1.3. Інтернет-магазин повинен складатися з таких розділів:{% for section in site_sections %}
1.3.{{ loop.index }}. {{ section }}{% endfor %}

1.4. Виконавець зобов'язується передбачити можливість управління з адміністративної панелі розділами сайту (додавати, редагувати, видаляти інформацію).

1.5. Типи користувачів:{% for role in user_roles %}
1.5.{{ loop.index }}. {{ role.name }} — {{ role.description }};{% endfor %}

1.6. Вимоги SEO:{% for req in seo_requirements %}
1.6.{{ loop.index }}. {{ req }};{% endfor %}
 
	 

1.7. Додаткові функції:

1.7.1. Адміністрування:{% for item in admin_functions %}
{{ loop.index }}. {{ item }}{% endfor %}

1.7.2. Функції користувача:{% for item in user_functions %}
{{ loop.index }}. {{ item }}{% endfor %}

2. Макет Інтернет-сайту:

2.1. Макет повинен бути виконаний переважно в тонах палітри: {{palette_description}}.   

Основний фон — {{background_color}}.

Шрифти — {{fonts_info}}..

2.2. {{block_structure_note}}

2.3. У дизайні макета присутня назва і логотип. {{logo_note}} 

2.4.  Макет повинен бути розроблений для всіх типів пристроїв: {{device_types}}. 
Сайт повинен бути адаптивним. Примітка: {{old_devices_note}}..

Сайти, які подобаються:
{% for liked_site in liked_sites_list %}
{{ liked_site }} {% endfor %} 	Сайти, конкурентів:
{% for competitor_site in competitors_list %}
{{ loop.index }}. {{ competitor_site }}
{% endfor %}


2.5. Опис спрямованості сайту: {{site_purpose_description}}.

2.6. Цільова аудиторія — 
Категорії клієнтів:{% for audience_group in target_audience_groups %}
{{ loop.index }}. {{ audience_group }}{% endfor %}
 

3. Структура розділів інтернет-магазину

3.1. Всі сторінки сайту містять:
3.1.1. Верхній графічний блок — містить в собі: 
1. Логотип {% if logo_note %}{{ logo_note }}{% endif %}
2. Блок «Основне меню» — {{ main_menu_description }}
{% if main_menu_note %}{{ main_menu_note }}{% endif %}
3. Телефон — {{ phone_numbers_list }}
4. Посилання на месенджери:
{{ messengers_list }} {% if messengers_note %}{{ messengers_note }}{% endif %}
5. Блок «Пошук» — при натисканні на яку відбувається перехід на вкладку «Обране» особистого кабінету де відображені товари, які були додані в обране. {% if search_note %}{{ search_note }}{% endif %}
6. Кнопка «Обране» — {{ favorites_description }} {% if favorites_note %}{{ favorites_note }}{% endif %}
7. Блок «Корзина» — відображається кількість товарів доданих користувачем в корзину для оформлення замовлення. {% if cart_note %}{{ cart_note }}{% endif %}
8. Блок «Вхід / Реєстрація» — для неавторизованого користувача, «Особистий кабінет/Вихід» - для авторизованого користувача. При натисканні на кнопку «Вхід» користувачеві відкривається форма з полями для заповнення:
1.Номер телефону
2. Пароль
3. Кнопка «Вхід»
{% if auth_note %}{{ auth_note }}{% endif %}{% if header_notes %}
Побажання щодо хедера:{{ header_notes }}{% endif %}


Процес реєстрації описаний в п. 3.8

3.1.2. Нижній текстовий блок — містить в собі: {% for element in footer_elements %}
{{ loop.index }}. {{ element.name }} — {{ element.description }}{% if element.components %}{% for comp in element.components %}
   {{ loop.index }}.{{ loop.index0 + 1 }} {{ comp.name }} — {{ comp.data }}{% endfor %}{% endif %}{% if element.developer_notes %}
   Уточнення / побажання: {{ element.developer_notes }}{% endif %}{% endfor %}

1. Копірайт
2. Контакти
3. Блок «Основне меню» ¬— {% for section in site_sections %}
{{ loop.index }}. {{ section }}{% endfor %}
4. Телефон {% if footer_phone_list %} — {{ footer_phone_list }}{% endif %}
5. Посилання на месенджери: {% if footer_messengers_list %}{{ footer_messengers_list }}{% endif %}
6. Електронна адреса — {{ email }}
7. Графік роботи — {{ schedule }}
8. Адреса{% if footer_address %} — {{ footer_address }}{% endif %}
{% if footer_address_note %}{{ footer_address_note }}{% endif %}
9. Ми приймаємо до сплати:{% if footer_payment_info %} {{ footer_payment_info }}{% endif %}
{% if footer_payment_note %}{{ footer_payment_note }}{% endif %}

10. Кнопка «На гору» {% if footer_notes %}
Побажання щодо футера: {{ footer_notes }} {% endif %}

3.2. Головна сторінка сайту містить в собі блоки:
Блоки вказані в пункті 3.1.
{% for block in main_page_blocks %}
3.2.{{ loop.index }}. {{ block.name }} — {{ block.description }} {% if block.client_note %}
Уточнення від Замовника: {{ block.client_note }}
{% endif %}{% endfor %}

 
{% for section in dynamic_sections %}
3.{{ loop.index }}. {{ section.title }} — {{ section.description }}{% if section.notes %}
Уточнення від Замовника:{% for note in section.notes %}
   {{ loop.index }}.{{ loop.index0 + 1 }} {{ note }}{% endfor %}{% endif %}{% if section.filters %}
Фільтри:{% for filter in section.filters %}
   {{ loop.index }}.{{ loop.index0 + 1 }} {{ filter.name }} — {{ filter.description }}{% endfor %}{% endif %}{% if section.sorting %}
Сортування:{% for sort_item in section.sorting %}
   {{ loop.index }}.{{ loop.index0 + 1 }} {{ sort_item.name }} — {{ sort_item.description }}{% endfor %}{% endif %}{% if section.components %}
Компоненти/поля:{% for comp in section.components %}
   {{ loop.index }}.{{ loop.index0 + 1 }} {{ comp.name }} — {{ comp.description }}{% if comp.subcomponents %}
      Підпункти:{% for subc in comp.subcomponents %}
         {{ loop.index }}.{{ loop.index0 + 1 }} {{ subc.name }} — {{ subc.description }}{% endfor %}{% endif %}{% if comp.client_notes %}
      Уточнення / побажання:{% for cn in comp.client_notes %}
         {{ loop.index }}.{{ loop.index0 + 1 }} {{ cn }}{% endfor %}{% endif %}{% endfor %}{% endif %}{% if section.subsections %}
Підрозділи:{% for sub in section.subsections %}
   {{ loop.index }}.{{ loop.index0 + 1 }} {{ sub.title }} — {{ sub.description }}{% if sub.filters %}
      Фільтри:{% for filter in sub.filters %}
         {{ loop.index }}.{{ loop.index0 + 1 }} {{ filter.name }} — {{ filter.description }}{% endfor %}{% endif %}{% if sub.sorting %}
      Сортування:{% for s in sub.sorting %}
         {{ loop.index }}.{{ loop.index0 + 1 }} {{ s.name }} — {{ s.description }}{% endfor %}{% endif %}{% if sub.fields %}
      Поля:{% for f in sub.fields %}
         {{ loop.index }}.{{ loop.index0 + 1 }} {{ f.name }} — {{ f.description }}{% if f.subfields %}
            Підполя:{% for sf in f.subfields %}
               {{ loop.index }}.{{ loop.index0 + 1 }} {{ sf.name }} — {{ sf.description }}{% endfor %}{% endif %}{% if f.client_notes %}
            Уточнення / побажання:{% for fn in f.client_notes %}
               {{ loop.index }}.{{ loop.index0 + 1 }} {{ fn }}{% endfor %}{% endif %}{% endfor %}{% endif %}{% if sub.client_notes %}
      Уточнення / побажання:{% for sn in sub.client_notes %}
         {{ loop.index }}.{{ loop.index0 + 1 }} {{ sn }}{% endfor %}{% endif %}{% endfor %}{% endif %}{% endfor %}


 

3. Оптимізація, швидкість завантаження, чистота коду.
3.3. Всі форми на сайті повинні містити перевірку на заповнення полів і видавати конкретні помилки в разі, якщо поле не було заповнено користувачем

3.4. При включенні сайту на обслуговування:
3.4.1. сайт повинен коректно відображатися для користувачів
3.4.2. на сайті повинен відображатися індивідуальний текст, із зазначенням причини переведення сайту на тех. Обслуговування.

3.5. При переході на неіснуючу сторінку, на сайті повинна відображатися помилка 404. На даній сторінці повинно відображатися меню сайту і привабливе зображення.
3.6. Всі непотрібні для роботи сайту модулі повинні бути вимкнені і видалені.

3.7. Головна сторінка сайту і основні розділи сайту повинні бути валідними.

3.8. Для швидкості завантаження:
3.8.1. CSS має бути стиснута до мінімальної кількості файлів
3.8.2. JS повинен бути стиснутий до мінімальної кількості файлів
3.8.3. Завантаження JS повинна бути перенесена в кінець файлу
3.9. Для подальшого просування сайту повинен бути встановлений комплект SEO модулів виконавця.

3.10. В меню адміністративної панелі для зручності виконавець розміщує всі потрібні для редагування матеріалів сайту посилання.

3.11. З зовнішньої частини сайту, при натисканні на ctrl, для адміністратора повинна бути можливість швидкого редагування основних блоків, заголовків, текстів і назв.

3.12. Сайт повинен бути адаптивним, повинен коректно відображатися на всіх мобільних пристроях Android, iPhone і iPad.






 

4. Адміністративний розділ

Адміністративний розділ — це список розроблених Виконавцем php-сторінок, що дозволяють Замовнику змінювати налаштування php-програми, задавати умови реєстрації (анкетування) користувачів, додавати Замовнику всілякі матеріали в усі розроблені Виконавцем розділи сайту, відключати сайт на техобслуговування, змінювати будь-яку інформацію на сайті, розміщувати будь-яку рекламно-текстову інформацію на сторінках сайту, а так само інші технічні функції, що дозволяють управляти і налаштовувати Інтернет-портал Замовника в необхідної для нормальної роботи сайту.

4.3. Логін і пароль для входу в Адміністративний розділ передаються Замовнику Виконавцем після повної оплати роботи Виконавця.

4.4. Виконавець не несе відповідальність за втрату Замовником пароля входу від адміністративної частини (після передачі логіна і пароля Замовника).

4.5. Адміністратор сайту має можливість редагування всієї інформації сайту, в тому числі і назви сторінок.
